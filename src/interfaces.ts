export interface IArticle {
  alias: string;
  createdAt: string;
  description: string;
  title: string;
  updatedAt: string;
}

export interface IBox {
  alias: string;
  createdAt: string;
  description: string;
  title: string;
  updatedAt: string;
}
